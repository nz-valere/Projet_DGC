# URL de base de l'API FastAPI (sans slash final)
VITE_API_URL=http://localhost:8001

# Mode d'authentification mock : "true" tant que le backend n'expose pas /auth/login.
# Permet de choisir son rôle sur la page de connexion pour tester chaque interface.
VITE_AUTH_MOCK=true
